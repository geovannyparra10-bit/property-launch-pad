import "@/styles/globals.css";

export const metadata = {
  title: "Property Launch Pad",
  description:
    "Calculators, analyzers, and tools for smarter property investment decisions.",
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
